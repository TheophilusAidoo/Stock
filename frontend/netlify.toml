[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Next.js routing - all routes should go through Next.js
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/next"
  status = 200
